name: hello-world

on: push

jobs:
  hello-world-job:

    runs-on: ubuntu-latest

    steps:
      - name: check out repository code
        uses: action/chcekout@v4
      -run: echo "$(cat hello-world.txt)"
      
        
